{
  "name": "wos-project",
  "version": "0.1.1",
  "description": "Project lifecycle: new, import existing workspace, checkpoint, resume, complete. Handles both long projects and one-off tickets. Auto-resumes on SessionStart and writes session-summary notes on Stop.",
  "skills": "./skills/",
  "hooks": "./hooks/hooks.json",
  "interface": {
    "displayName": "Workflow OS — Project",
    "shortDescription": "Project lifecycle, selective import, auto-resume, and auto-checkpoint",
    "category": "Productivity"
  }
}
